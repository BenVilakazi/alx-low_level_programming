#include <stdio.h>
#include "main.h"
/**
 * _abs - Computes Absolute val of an int
 * @a:The int to Check
 * Return: The Absolute val of an int
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	return (-a);
}
