#include <stdio.h>
#include "main.h"
/**
 * add - Adds 2 int & returns the res
 * @a: num being added
 * @b: num being added
 * Return: (0) indicates succeesful execution.
 */
int add(int a, int b)
{
	int res;

	res = a + b;
	return (res);
}
